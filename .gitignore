__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store
.idea/
.vscode/

# Environnements virtuels, quel que soit leur suffixe (.venv-torch, .venv-py312...)
.venv*/

# Sorties de campagne : les mesures (metrics.csv, config.json) sont versionnees,
# les checkpoints intermediaires non - 78 Mo pour 408 fichiers. Les modeles
# retenus pour le rendu sont copies dans models_livrables/.
runs*/**/*.pt
